const encode = 'encode';
const decode = 'decode';
const types = [encode, decode];
const alphabet = 'abcdefghijklmnopqrstuvwxyz';
const mod = alphabet.length;

module.exports = {
  encode,
  decode,
  types,
  alphabet,
  mod
};
